import React from 'react';

const ForumTest = () => {
    console.log('ForumTest component rendered');

    return (
        <div style={{ padding: '20px', background: '#333', color: '#fff', minHeight: '100vh' }}>
            <h1>Forum Test Component</h1>
            <p>This is a test component to verify forum routing is working.</p>
        </div>
    );
};

export default ForumTest;
